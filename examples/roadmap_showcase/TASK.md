# Goal

Improve the numeric score while preserving a small, readable multi-file workspace.

Use exact SEARCH/REPLACE mutations. The evaluator reports `size` and `kind` metrics for multi-objective selection and MAP-Elites.
